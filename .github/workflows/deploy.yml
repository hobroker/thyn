name: Deploy

on:
  push:
    branches:
      - master
      - stage

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy script
        run: >
          ls scripts
          bash ./scripts/deploy.sh

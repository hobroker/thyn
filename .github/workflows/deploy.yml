name: Deploy

on:
  push:
    branches:
      - master
      - stage

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Docker ps
        run: docker ps -a
    env:
      DOCKER_HOST: ${{ secrets.DOCKER_HOST }}
